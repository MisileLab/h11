20:        // Truncate command to 80 characters if needed
21:        val displayText = if (command.length > 80) {
22:            command.substring(0, 77) + "..."
43:        drawContext.fill(x - 4, y - 4, x + textWidth + 4, y + 12, 0x80000000.toInt())
